import React from 'react';
import styles from '../stylesheets/DateHeader.module.scss';

const DateHeader = (props) => {
  return <div className={styles.default_date_header}></div>;
};

export default DateHeader;
